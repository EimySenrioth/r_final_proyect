# Instalar y cargar librerías para estadísticas descriptivas
if (!require("summarytools")) install.packages("summarytools", repos = "https://cloud.r-project.org")
if (!require("psych")) install.packages("psych", repos = "https://cloud.r-project.org")
if (!require("dplyr")) install.packages("dplyr", repos = "https://cloud.r-project.org")
if (!require("skimr")) install.packages("skimr", repos = "https://cloud.r-project.org")
if (!require("rpart")) install.packages("rpart", repos = "https://cloud.r-project.org")
if (!require("rpart.plot")) install.packages("rpart.plot", repos = "https://cloud.r-project.org")
if (!require("caret")) install.packages("caret", repos = "https://cloud.r-project.org")
